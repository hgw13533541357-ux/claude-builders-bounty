# PR Review: mem0ai/mem0#4630
> **fix: Pass embeddingDims to OpenAI embeddings API** by @hgw13533541357-ux
> 1 file | +4/-1
---
## Summary
This PR fixes a bug where `embeddingDims` is stored in the config but never passed to the OpenAI `embeddings.create()` call, causing dimension mismatch errors for configurable embedding models. The fix passes `dimensions: this.embeddingDims` to both `embed()` and `embedBatch()` methods.

## Risks
- Low risk: the change is minimal (4 lines) and purely additive — it passes an already-available config value to the API call
- Backward compatible: `dimensions` parameter is optional in the OpenAI API; passing it only affects behavior when `embeddingDims` is explicitly set

## Suggestions
- Consider adding a validation check that `embeddingDims` is a positive integer greater than 0
- The PR description has a broken link format (`#<!-- issue number -->`) — minor cleanup

## Confidence
**High** — clear bug, minimal change, correct fix
---
*Generated by claude-review*
